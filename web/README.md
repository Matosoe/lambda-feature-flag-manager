# Site Estático SSO

Esta pasta contém o site estático HTML+JS para simular o login SSO (OIDC).

## Como rodar localmente
Abra o index.html em seu navegador.

## Deploy automatizado
Execute o script deploy.sh para publicar no S3 (consulte o PLAN.md para detalhes de configuração).
