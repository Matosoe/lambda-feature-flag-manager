# Lambda - Feature Flag Manager

Esta pasta contém o código da Lambda, Swagger UI e scripts de deploy.

## Como rodar/testar localmente
Consulte o PLAN.md e o README da infra/local para subir o ambiente.

## Deploy automatizado
Execute o script deploy.sh para publicar a Lambda e Swagger UI no S3.
